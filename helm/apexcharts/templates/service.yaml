apiVersion: v1
kind: Service
metadata:
  name: {{ include "apexapp.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "apexapp.name" . }}
